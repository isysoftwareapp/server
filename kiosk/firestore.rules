rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT MODE - Allow all operations without any restrictions
    match /{document=**} {
      allow read, write, create, update, delete: if true;
    }
  }
}
